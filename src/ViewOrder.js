import {useState} from "react";

const ViewOrder = ({user}) => {
    //useParams to get Order ID
    //display order details (as long as user === userId on order)
    //states


    //

    //
    return <div>
        View Order
    </div>
};

export default ViewOrder;